# 0911 项目后续工作交接表

**项目**：fcmsafety —— 食品接触材料（FCM）化学物质安全筛查 R 包
**交接日期**：2026-09-11　**最近更新**：2026-09-11 深夜
**当前分支**：`fcmsafety-v2`　**最新提交**：`215e04c`
**代码自检**：`R CMD check --no-install` → **0 ERROR / 1 WARNING**（既有的 `R/*.R` 非 ASCII 字面量，已接受不修）
**测试**：**270 用例 / 0 失败 / 0 错误**（`NOT_CRAN=true tools/run_tests.R`）

> ⚠️ **`library(fcmsafety)` 里的代码停在 2026-09-11 09:29**，之后的 `df6297a`（更新按钮两段式）
> 与 `215e04c`（`run_screening()`）**都还没装进去**。见 P0-1。

---

## 〇、接手第一件事（30 分钟速览）

按这个顺序做，不要跳：

| 步骤 | 做什么 | 在哪 |
|---|---|---|
| 1 | 读 `docs/工作总结-20260911.md`（9/9–9/11 三条主线的完整总结，含全部关键数字） | 仓库根目录 |
| 2 | 读 `.workbuddy/memory/MEMORY.md`（**长期笔记：规则 / 坑 / 待办 / 结论**，最浓缩） | `.workbuddy/memory/` |
| 3 | 读本文档第二节「待办清单」，认领要做的项 | 本文 |
| 4 | 跑一次 `NOT_CRAN=true tools/run_tests.R` 确认基线 | VSCode Git Bash |
| 5 | **重装 R 包**（见待办 P0-1，否则你的改动看不见） | VSCode Git Bash |

**重要背景**：`.workbuddy/memory/2026-09-*.md` 是逐日工作日志（最详细，65KB/天），排查具体问题时的第一手资料；`MEMORY.md` 是提炼后的规则和坑。遇到"为什么当初这么改"的疑问，**先翻日志再下结论**，不要凭代码猜。

---

## 一、项目定位与真实状态

### 1.1 一句话现状

核心链路（**输入 → 结构补全 → 法规匹配 → 毒性定级 → Excel 报告**）**已经打通并在真实库上跑通**；
法规库自动更新线从"从未真实联网跑通"推进到"**四库 diff 全部收敛（modified → 0）**"；
真库目前处于**半程状态**——A1/A1B/A2/A3/A5 已写入，**A4（eu_sml）被安全阀拦下等签字**。

### 1.2 项目在做什么

把用户检出的化学物质，与本地 SQLite 里从各法规来源派生的表做匹配，判断是否属于 SVHC / CMR / IARC / EU SML 等监管清单，再结合 Toxtree Cramer 规则给出 I–V 级毒性定级，最后导出可交付的 Excel 报告。

### 1.3 数据通道

**SQLite 为唯一数据通道**，旧的 xlsx 模式已于 2026-09-04 退役。真库位置：`inst/fcmsafety.db`。

### 1.4 四个核心数据来源（不是 8 个）

| 来源组 | 数据库 | 命名空间里的更新入口 |
|---|---|---|
| SVHC（ECHA 高关注物质） | `svhc` | `update_svhc_auto()` |
| CLP / Annex VI | `cmr`, `cmr_suspect` | `update_cmr_auto()` / `update_cmr_suspect_auto()` |
| IARC | `iarc` | `update_iarc_auto()` |
| EU 10/2011 | `eu_sml`, `eu_sml_group` | `update_eu_sml_auto()` |

总调度：`update_database_auto()`（薄调度，转发各 `update_*_auto()`，**单库失败不中断其他库**）。
China SML 无稳定公开在线源，走人工文件导入，不在第一阶段自动更新范围内。

---

## 二、待办清单（按优先级，可直接认领）

### P0 —— 阻塞性，必须先做

| # | 事项 | 为什么重要 | 怎么做 | 状态 |
|---|---|---|---|---|
| P0-1 | **重装 R 包** | `library(fcmsafety)` 里停在 **9/11 09:29**，之后的 `df6297a`（更新按钮两段式）与 `215e04c`（`run_screening()`）**完全不可见**。你会以为改动没生效 | 在 **VSCode 的 Git Bash 干净终端**里，`cd` 到仓库根目录，跑 `LC_ALL=zh_CN.UTF-8 R CMD INSTALL .`。验证：`Rscript -e 'library(fcmsafety); exists("run_screening", where = "package:fcmsafety")'` 应为 `TRUE` | ⬜ 待做 |
| P0-2 | **eu_sml 3 行 removed 待签字** | A4 被安全阀拦下，`eu_sml` 保持 **681 未动**，与源不一致 | 见下方「P0-2 详情」，需你/导师做产品语义决策 | ⛔ 等拍板 |

**P0-2 详情**

A4 的执行结果是 `success=FALSE wrote=FALSE | added=21 removed=3 modified=45 unassigned=205`。
`query_*()` 的 `removed` 已存到 `.workbuddy/.res_eu_sml.rds`。被拦下的 3 行：

| FCM 编号 | 名称 | CAS | 情况 |
|---|---|---|---|
| 121 | salicylic acid | 69-72-7 | 源里 FCM 编号 `120 → 122` **直接跳过 121** |
| 151 | 2,2-bis(4-hydroxyphenyl)propane（双酚 A / BPA） | 80-05-7 | 源里无，三个 CAS 全无 |
| 154 | 4,4′-dihydroxydiphenyl sulphone（双酚 S / BPS） | 80-09-1 | 源里无，三个 CAS 全无 |

已调真实 `fetch_eu_sml_data()` 核实（源 904 行），**确认是真上游移除**，不是解析 bug。背景：欧盟 2024/3190 禁 BPA 后将其从 10/2011 正面清单删除。

**两种处置，代价不同，需要拍板：**

| 选项 | 做法 | 结果 | 代价 |
|---|---|---|---|
| **删** | 直接跑 A4，让 eu_sml 与源一致 | 库与法规源完全对齐，可追溯性最强 | 筛查工具**不再对 BPA/BPS 告警**——这两个是高频关注物质，漏报风险大 |
| **留**（推荐） | 走白名单：把 3 个键登记为 `flag='upstream_removed_kept'` + `status='accepted'`，再跑 A4 | 库与源"标注过的"不一致，BPA 仍会告警 | 库里存在 3 行源里没有的记录，需在报告里说明 |

白名单机制已实现：`query_kept_removals()` / `drop_kept_removals()` 会把白名单里的键从 `changes$removed` 剔除**并改写 `total_removed`**（安全阀看的是这个计数）。白名单**存库里，不写死在代码里**。

### P1 —— 能力缺口

| # | 事项 | 说明 | 怎么做 |
|---|---|---|---|
| P1-1 | ~~**`run_screening()` 总控入口没做**~~ | ✅ **已完成**（提交 `215e04c`，ADR 0012）。`run_screening()` 把两步串成一次调用，只调度不复制逻辑 | `R/run_screening.R` + `tests/testthat/test-run-screening.R`（6 例） |
| P1-2 | **UVCB 条目被赋单体结构键** | `cmr` 的 648/649/650 段共 12 行全带 InChIKey，**其中 9 行是错的**（如 `Hydrocarbons, C4-5` 被赋成丁烷、`Alkanes, C1-2` 赋成乙烷、`stoddard solvent` 赋成 `C8H17BrO3`）。已用 `data_quality_flags` 可见化（9 条 `open`），**但结构没改** | **这 9 行现在仍会被 `assign_toxicity()` 拿错误 SMILES 跑 Cramer**，结论不可信。需逐行人工核定：清空错误键（入账本）还是保留。清单在 `.uvcb_structure_conflicts`。**改数据需签字** |
| P1-3 | **同一 InChIKey 共用导致误分级** | 已查清未动。9/11 实测：**cmr 9 组 / iarc 30 组**同键多行。iarc `OKTJSMMVPCPJKN` 共 **7 行**（Carbon black / MWCNT-7 / MWCNT other…）→ 官方 3 被定成 2B（**偏高**）；`FPAFDBFIGPHWGO` 3 行（Talc / Talc containing asbestos / Talc-based body powder）→ 官方 1 被定成 2B（**偏低**）。`BZHJMEDXRYGGRV` = PVC 与 Vinyl chloride 共用；cmr `IJDNQMDRQITEOD` = butane 与 isobutane 共用 | 「取最严」规则本身没错，**错在上游把不同物质塞进同一个键**。要么按 key+name 复合匹配，要么对这几类做特殊处理。先看 `.workbuddy/memory/MEMORY.md` 对应段落 |
| P1-4 | **CMR A 类组条目漏报**（9/11 晚已查清全貌） | **清单修正：24 条，不是 20 条**（判定规则见 `docs/cmr-group-audit-20260911.md`，22 条对主表不可见）。**漏报分两种，性质完全不同**：<br>**类型 A（物质在库内）→ 基本不影响定级。** 铅的 33 个库内物质 **32 个已是 V 级**（SVHC 兜住）；Cr 23 个、Sn 40 个、As 15 个同理。后果只是 `CMR` 列显示 `-`，**报告不精确但级别对**。<br>**类型 B（物质不在 `chemicals` 表）→ 真漏。** 实测硫酸肼/二盐酸肼/氯化铍/铬酸锌：`CMR`/`SVHC`/`IARC`/`EU_SML`/`China_SML` **全 `-`，`Toxic_level = "-"`，`Group_hits` 也 `-`**（官方均应 V）。**`-` 与"查过确实干净"不可区分** | **分界线是"物质在不在库内"，不是"注册表建没建"**。机制：关键词来自按 InChIKey **回查库内名称**（`group_membership.R` 1110–1150），库外物质拿不到名称 → 连 Group_hits 都空。<br>**最该修的实例**：`607-230-00-6 2-ethylhexanoic acid and its salts`（H360D/Repr.1B）—— 库里 2-乙基己酸的 **Ca/Mg/Zn/Ce/Sn 五种盐只有 `China_SML`（II/III，应 V）**，而这些是 **PVC 热稳定剂**，且**没被 SVHC 兜住** → 实打实漏级。<br>**判据可行性**：元素型（Pb/Be/As）可靠；骨架型（2-乙基己酸盐、芳胺盐、联苯胺系偶氮染料）SMARTS 可靠；**Cr(VI) 不可判**（`CrO3` 与 `Cr2O3` 都含 `[Cr]=O`）→ 只能人工白名单。**待拍板**：做全 24 条还是先做 FCM 高相关少数几条；命中后是否参与定级 |

### P2 —— 已知遗留与清理

| # | 事项 | 说明 |
|---|---|---|
| P2-1 | 组条目遗留 | ① `(see X)` 剩 9 行上游条目缺失；② `Nickel, metallic` / `Chromium, metallic` / `Cobalt metal without tungsten carbide` 是 `form` 层却无 `smiles_pattern`，**恒 `manual_review`，约 62 行噪音** |
| P2-2 | iarc 无 CAS 组条目的 diff 键风险（**未修**） | 源内 217 行无 CAS，diff 键退到 `agent`（自由文本）→ **上游改个名字 = 删一行 + 加一行**。iarc 源无稳定 ID 列可换，**只能接受**（不要再花时间修） |
| P2-3 | 清理临时脚本 | `tools/` 下有 **60+ 未跟踪的 `_tmp_probe*.R` / `_tmp_apply81.R` / `_tmp_measure_cmr.R` / `_tmp_verify_cmr.R` / `_tmp_diff_report.py`**（git status 共 70 项未跟踪）。**未获批准，不要清理**。若清理，见下节红线 |
| P2-4 | 重装后重跑全流程 | 重装包后，跑一次全库端到端（2446→2606 行）确认定级分布没漂移 |

---

## 三、⛔ 开发红线（违反会出事，必读）

这些不是建议，是**踩过坑换来的**：

| 红线 | 具体内容 | 为什么 |
|---|---|---|
| **分支** | 只在 `fcmsafety-v2` 开发，**绝不改 `main`** | 项目约定 |
| **禁止 `git stash` / `git rm` / 裸 `rm`** | 清临时文件一律 `mv` 到 `.workbuddy/` | safe-delete 钩子**两次清空过 `.git/objects`**。9/11 有人用 `rm -f` 删了自己的临时脚本，虽事后 `git fsck` 确认仓库完好，但这是运气 |
| **禁止 `DELETE FROM table` 全量重写** | 新代码里不得用整表刷新 | 这是旧 `R/databases.R` 的问题，别扩散 |
| **`R/databases.R` 的 `download_latest_clp()` / `update_cmr()` 已过时** | **不要用** | 全量重写 + 下载后不筛 H 代码。CLP 派生 CMR 的**唯一正确入口**是 `R/update_other_dbs.R` 的 `update_cmr_auto()` |
| **不要丢弃没有 InChIKey 的法规条目** | regulatory 表保留完整条目，`InChIKey` 可为空；匹配只对有键的行做结构匹配 | 大量 UVCB / 组条目无结构但**有法规意义** |
| **匹配和更新必须分离** | `assign_toxicity()` 不得默认联网、不得默认改库 | 当前 `R/direct_sql_toxicity.R` 里还残留着"默认检查更新"的旧设计，改到这儿时注意 |
| **写库前必须备份** | `apply` 前自动备份 + 事务 + 全部成功或全部回滚 | 数据完整性第一 |
| **标记类数据放旁路表** | 别往业务表的 `notes` 里写标记 | `write_changes_to_db()` 是 DELETE + INSERT，源 df 里没有的列一律填 NA → 该行日后被判 modified，**标记被静默抹掉** |
| **审计一律调真实函数** | 不要旁路复刻逻辑 | 曾用 Python 复刻 IARC 匹配逻辑，漏了 `layer` 判定 → 得出"58 行误升 V 级"的**错误结论**。旁路复刻的偏差会变成看起来同样有说服力的错误结论 |
| **抽样不能外推** | 用抽样推总体前先 `order by random()` | 从账本 `LIMIT 20` 抽样推出"764 行大半被限流吞掉"，**实际只有 57 行（7.4%）**。账本按插入序存 = 源行序 = CLP 段序，与物质类型强相关 |

---

## 四、本机环境坑（WorkBuddy 特有，不解决寸步难行）

| 现象 | 真因 | 怎么做 |
|---|---|---|
| 退出码 **139 / 0xC0000005** | WorkBuddy 沙箱 `tsbx.dll` **运行时注入**子进程，退出期钩子与 rlang/cli 析构回调冲突。**不是 R 的 bug，不是应用 bug** | **不要重装/降级 R，不要改 WorkBuddy 安装目录**。看落盘产物判成败。要干净退出码就在 VSCode Git Bash 里跑，或脚本末尾 `tools::pskill(Sys.getpid())` |
| `R CMD check` 报奇怪错误 | 沙箱环境 | **必须加 `--no-install`**（结果照样完整写出，只是 examples/tests 显示 SKIPPED） |
| 含中文文件名的包源码复制失败 | `LC_ALL=C` 导致静默失败 | **必须 `LC_ALL=zh_CN.UTF-8`** |
| 长内联 `Rscript -e` 崩溃 | 沙箱 | 写成临时 `.R` 文件再跑，**别放 `/tmp`**。查 SQLite 用 Python `sqlite3`。长驻进程后台跑 |
| 1 个 WARNING 一直存在 | `R/*.R` 里的非 ASCII 字面量 | 已接受不修，**不要试图修** |

**R 位置**：`/c/Program Files/R/R-4.6.1/bin/Rscript.exe`（R 4.6.1）
**终端**：VSCode 默认是 **Git Bash**（不是 PowerShell），命令用 Bash 语法

---

## 五、常用命令速查

```bash
# 全部在仓库根目录、VSCode Git Bash 里跑
cd "C:/Users/13432/WorkBuddy/2026-08-31-14-16-57/fcmsafety"

# 跑全部测试（必须 NOT_CRAN=true）
NOT_CRAN=true LC_ALL=zh_CN.UTF-8 "/c/Program Files/R/R-4.6.1/bin/Rscript.exe" tools/run_tests.R

# 跑单个测试文件
NOT_CRAN=true LC_ALL=zh_CN.UTF-8 "/c/Program Files/R/R-4.6.1/bin/Rscript.exe" tools/run_one_test.R test-cas-candidates.R

# 包自检（必须 --no-install）
LC_ALL=zh_CN.UTF-8 "/c/Program Files/R/R-4.6.1/bin/Rscript.exe" tools/run_check.R

# 重装包（改码后必做，否则改动看不见）
LC_ALL=zh_CN.UTF-8 "/c/Program Files/R/R-4.6.1/bin/R CMD INSTALL ."

# 重新生成文档（改了 roxygen 注释后）
LC_ALL=zh_CN.UTF-8 "/c/Program Files/R/R-4.6.1/bin/Rscript.exe" -e 'roxygen2::roxygenise(".")'
```

**法规库更新线体检三件套**（**只写副本库 `%TEMP%/fcm_auto_test/`，真库零改动**）：

| 脚本 | 用途 |
|---|---|
| `tools/test_auto_update_line.R` | 一次跑完下载层 → 增量 diff → 总调度，回答"这条线现在还通不通" |
| `tools/diagnose_diff_columns.R` | 某个库为什么被判一大片 modified？列级诊断 |
| `tools/rehearse_full_alignment.R` | 全量对齐演练。**要 `FCM_REHEARSE_ENRICH=TRUE` 才看得到真实 added**，否则新增行全被摘走、added 恒 0 |

**组条目相关**：`tools/audit_group_hits.R`（命中分布）、`tools/compare_group_membership.R`（开关 group_membership 对最终定级的影响对比）

---

## 六、法规库自动更新线：你必须知道的机制

### 6.1 链路

```
update_database_auto()
  → update_*_auto()
    → run_incremental_update()
        map（列映射）→ 回填 → diff（键比对）→ 确认（安全阀）→ write_changes_to_db
```

### 6.2 三处硬约束（决定了"为什么装不完"）

1. 四个公共源的官方导出**都不含 InChIKey 列**
2. 四张业务表 `InChIKey` **NOT NULL** + 外键指向 `chemicals`
3. `backfill_meta_from_db()` **只查同一张业务表**，对新增行无用

⇒ **新增行只能靠 `enrich_new_compounds()` 联网 PubChem**。

**所以 `cmr` / `iarc` "装不完"是设计如此，不是 bug**：`cmr` 源 1150 行里 **705 行是 UVCB 或 A 类组条目**，`InChIKey NOT NULL` 决定了它们进不来。这是"跳过并记账、不放宽约束"的有意选择。

### 6.3 安全阀

**`changes$total_removed > 0` → 非交互模式一律 `Cancelled`**。这是防误删的最后一道闸门。

绕过方式只有一个：**先把键登记进白名单**（`flag='upstream_removed_kept'` + `status='accepted'`），`drop_kept_removals()` 会把它从 removed 里剔除并改写 `total_removed`。

### 6.4 假变更的首要来源

**列映射对不上**。CLP 官方列名是"分组名 + 子列名"的扁平长名，必须在 `db_col_candidates` 里显式登记，且 **Labelling 要排在通用词之前**。历史上 `hazard_statement_codes` **从来没被更新过**，就是因为这个。

### 6.5 其他要点

- **diff 与回填必须同一套键定义**（`key_of_df()`）；多行单元格按行排序比较（`canon_cell()`）；`write_changes_to_db` 是"按 key 全删再插"，**多行键会净丢行**
- `enrich` **默认 TRUE**（四个源统一）；`update_database_auto(enrich = NULL)` 尊重子库默认
- **PubChem 重试**：429/5xx/断连按 1s→2s→4s 退避 3 次；404/400 立即返回。`pubchem_lookup_cas()` 带 `lookup_status ∈ ok/not_found/unavailable`
- **这个重试是必须的**：修之前，一次网络抖动 = 该行**永久判"无结构"**，764 行被静默吞掉（实际救回 57 行，含敌草隆 / 1,4-二氧六环 / DEGME / TPO）
- 源文件读取：`source="local"` 读 `inst/clp_cmr_meta.xlsx`（**旧 meta**）；**写真库一律显式传 `new_file=`**

---

## 七、真库当前状态（9/11 15:12，基线 = A1 前备份）

| 表 | 基线 | 现在 | 新增 | 删除 | 业务内容变化 |
|---|---:|---:|---:|---:|---:|
| `cmr` | 332 | **443** | 113 | 2 | 330 |
| `cmr_suspect` | 359 | **451** | 101 | 6 | 349 |
| `iarc` | 859 | **896** | 40 | 1 | 821 |
| `eu_sml` | 681 | 681 | 0 | 0 | 0（**A4 被拦**） |
| `chemicals` | 2446 | **2606** | 160 | 0 | 0 |
| `svhc` | 294 | 294 | 0 | 0 | 0 |
| `unassigned_entries` | 0（新表） | **1039** | | | 未分配账本 |
| `data_quality_flags` | 9 | **12** | | | 9 UVCB 错误赋键（`open`）+ 3 裁定保留（`accepted`） |

**`unassigned_entries` 1039 行构成**：cmr 764（719 `no_structure_found` / 43 `no_cas` / 2 `lookup_unavailable`）、cmr_suspect 45、iarc 230（212 `no_cas` / 18 `no_structure_found`）。

**完整性**：`PRAGMA integrity_check = ok`、`foreign_key_check` 0 行、`cmr.InChIKey` 孤儿 0。

### 7.1 内容变化的真相（**不是"改数据"，是把旧脏值写回正确形态**）

迁移期引入的 bug，这次一次性修正。逐字段证据在 `change_log`：

| 库 | 变化的列 | 旧值 → 新值 | 性质 |
|---|---|---|---|
| `cmr` | `pictogram` 330 行 | `'Dgr\nGHS06\nGHS08'` → `'GHS06\nGHS08'` | 迁移期 `safe_get_col()` 子串匹配把信号词并进象形图列 |
| `cmr` | `signal_word_codes` 330 行 | `'Dgr\nGHS06\nGHS08'` → `'Dgr'` | 同上，**现在两列彻底分离** |
| `iarc` | `evaluation_year` 833 行 | 空 → 有值 | 迁移期写死 `NA_integer_` |
| `iarc` | `volume` 637 行 | 空 → 有值 | 迁移期 `as.integer()` 把多值文本截空 |

验证：`signal_word_codes` 含 GHS 的行 **0**、`pictogram` 含 Dgr/Wng 的行 **0**；`iarc.evaluation_year` 空值 896 → **只剩 27**、`volume` 只剩 25（剩下的是白名单保留行）。

### 7.2 本次真库写入的执行顺序（**有硬依赖，勿乱序**）

```
A1   删 cmr 2 行 → 写 cmr              ✅ 2 行确认真下架
A1B  修好重试后重跑写 cmr               ✅ 救回 57 行
A5   登记 3 行保留白名单 → accepted      ✅ 必须先于 A2 ——
                                          否则 removed>0 会把 A2/A3 判 Cancelled
A2   删 cmr_suspect 6 行 → 写            ✅ 359 → 451
A3   删 iarc 1 行（Aflatoxin M1）→ 写    ✅ 859 → 896
A4   写 eu_sml                          ⛔ 被拦：removed=3，等签字
```

**执行脚本**：`tools/_tmp_apply81.R`（用环境变量 `FCM_STAGE=A1|A1B|A5|A2|A3|A4` 选阶段）
**日志**：`.workbuddy/apply_real_db.log`

### 7.3 备份位置

| 备份 | 内容 |
|---|---|
| `.workbuddy/backup/fcmsafety.db.before-realdb-write.20260911-135814` | **= A1 前基线**（最重要的回滚点） |
| `.workbuddy/backup/fcmsafety.db.before-index-no.20260911-121347` | index_no 主键改动前 |
| `.workbuddy/backup/fcmsafety.db.before-dqflag.20260911-122453` | data_quality_flags 前 |
| `.workbuddy/backup/fcmsafety.db.before-cmr-completion.20260911-144527` | cmr 补齐前 |
| `backups/fcmsafety_20260911_*.db` × 6 | 自动备份 |

---

## 八、关键架构与概念（避免重复踩坑）

### 8.1 毒性定级 I–V（ADR 0007）

| 级别 | 判据 |
|---|---|
| **V** | SVHC / CMR H340,350,360 / EDC / IARC 1 / SML ≤ 0.018 |
| **IV** | H341,351,361 / CMR_suspect / IARC 2A·2B / 0.018 < SML ≤ 0.09 / Cramer III |
| **III** | 0.09–0.54 / Cramer II |
| **II** | 0.54–1.8 / Cramer I |
| **I** | 1.8–60 |

- **多条件取最严**
- **全无证据留空 `-`，不冒充 I 级**（重要：`-` 是中性灰，**绝不能用绿**）
- `compute_toxicity_levels()` 是纯函数；`summarise_*()` 先按 InChIKey 汇总（**同键多行取最严**）再对齐
- H 码写法不统一 → **按前 4 位归并**；`cmr.hazard_statement_codes` 分隔符是 `"\r\r\n"`

### 8.2 导出报告（ADR 0008）

- `output_file` **按扩展名分派**：`.xlsx` → `export_toxicity_report()`（4 张表：Results / Summary / Unassigned / Issues）；`.csv` → `write.csv`
- 配色：V 深红 → I 绿
- **失败可见化**：`query_*()` 出错挂 `attr(x, "query_error")` → 写进 Issues 表 + 控制台告警
- ⚠️ **别改成"全查不到就 `stop()`"** —— 静默失败比崩溃更危险，现在这样是有意设计

### 8.3 各法规的"组"分三类（**别混**）

| 类型 | 含义 | 命中方向 | 各库数量 |
|---|---|---|---|
| **A 覆盖型** | 一条代表一整类 | 命中组条目 → **整类适用** | IARC 39 / CMR 20 / SVHC 5 |
| **B 组限值** | 多条目共享限值 | **方向与 A 相反**，只用于取限值 | EU SML 38 组 / 126 行 |
| **C UVCB** | 本身多组分 | 特殊处理 | CMR 645 行（648/649/650 段）/ SVHC 17 行 |

- China SML（GB 9685）**无 A 类组条目**（1182 行皆具体物质）；EDC 也无组
- ⚠️ **CMR 20 条 A 类中 18 条从未进 `cmr` 主表**（见 P1-4）

### 8.4 IARC 组条目（ADR 0009 + 0010）

- `assign_toxicity(group_membership = TRUE)`，**默认 FALSE**
- **元素层只做存在性检验 = 必要条件，不能当充分条件**（这条是核心教训）
- 组条目识别 = **三源并集**：名称正则 ∨ `cas_no` 为空 ∨ `.iarc_extra_group_entries`（人工 15 条），扣 `.iarc_force_single`。注册表 24 → 39
- **刻意不做模糊词法启发式**（会吞掉 `Dichlorvos` 这类真物质）
- 限定词靠 `negative_condition` / `require_condition`；无判据条目 `layer="manual"` → **宁可不报**
- 只有 `auto_confirmed` / `probable` 参与定级
- `(see X)` 已接入（`apply_iarc_see_aliases()`）：**只填空缺、绝不覆盖**
- 已删错误映射：`Cyclamate="C"` / `Talc="Mg"`（元素层对有机族无解——**有机族的定义是结构，不是元素**）

### 8.5 结构标识补全（ADR 0004）

- `prepare_input()`：输入只需 **「名称 + SMILES」** 即补 InChIKey/CID/Formula/ExactMass
- **全离线**，六级阶梯；结果挂在 `attr(out, "prepare_report")`
- CDK 离线算 InChIKey = rJava 调 `rcdklibs` cont/ 的 cdk-inchi jar
- 归一 SMILES **必须 `Absolute` flavor**
- ⚠️ **调用顺序：先 `parse.smiles()` 再 `get.smiles(flavor=)`**

### 8.6 代码导航规范（改动前必读）

1. **roxygen 新块 `#' @encoding UTF-8` 放块尾**（ADR 0006），护栏 `test-rd-docs.R`
2. **往 R 文件插注释，锚点必须是 roxygen 块的 `#' 标题` 行** —— 插块中间会把文档块劈开，**代码照跑、测试全绿，只有 Rd 静默缺内容**。护栏 `tools/check_section_markers.R`
3. R 文件统一用 `# ---- 分区名 ----` 分区；`# ====` 只用于文件头
4. 索引在 `docs/code-map.md`

---

## 九、已导出的 API（NAMESPACE）

| 分类 | 函数 |
|---|---|
| **主流程** | `prepare_input()` / `print_prepare_report()` / `assign_toxicity()` / `export_toxicity_report()` / `run_toxtree()` / `export4toxtree()` |
| **组匹配** | `assign_group_membership()` / `assign_group_membership_table()` |
| **法规库更新** | `update_database_auto()` / `update_svhc_auto()` / `update_cmr_auto()` / `update_cmr_suspect_auto()` / `update_iarc_auto()` / `update_eu_sml_auto()` |
| **下载原语** | `download_clp()` / `download_svhc()` / `download_iarc()` / `download_eu_sml()` |
| **数据库管理** | `get_db_connection()` / `initialize_database()` / `setup_fcmsafety_database()` / `check_database_status()` / `validate_database_integrity()` / `migrate_xlsx_to_sqlite()` / `load_databases_sqlite()` / `get_database_statistics()` / `fcmsafety_status()` |
| **审计** | `get_update_history()` / `export_update_history()` |
| **人工清单** | `check_manual_lists()` |
| **Shiny** | `launch_database_inspector()` |

> ⚠️ **`run_screening()` 不存在**（见 P1-1）。

---

## 十、测试现状

- **25 个测试文件 / 270 用例，全绿**
- 测试文件覆盖：`assign-toxicity` / `cas-candidates` / `cas-canonicalization` / `change-log-detail` / `clp-split` / `cmr-screening` / `cmr-suspect-key` / `data-quality-flags` / `eu10-parser` / `fallback-keys` / `group-membership` / `initialize-database` / `kept-removals` / `key-normalisation` / `prepare-input` / `pubchem-retry` / `rd-docs` / `report-export` / `schema-rebuild` / `snake-schema-adaptation` / `toxicity-levels` / `toxtree` / `unassigned-entries` / `update-database-auto`
- **测试不需要联网，不写真实数据库**
- 新增功能请补测试（项目习惯：TDD 红→绿，如 `test-cas-candidates.R` 15 个用例）

---

## 十一、下一个小时该干什么（建议顺序）

1. **重装 R 包**（P0-1）—— 不然后面所有验证都是假的
2. `NOT_CRAN=true tools/run_tests.R` 确认 245 用例全绿
3. 读 `docs/工作总结-20260911.md` + `.workbuddy/memory/MEMORY.md`
4. 打开 `launch_database_inspector()` 看一眼真库长啥样
5. **拍板 P0-2（eu_sml 3 行）** —— 这是唯一阻塞在"等决策"上的事，越早越好
6. ~~挑 P1-1（`run_screening()`）开工~~ ✅ 已完成
7. **接下来挑一件事做**，剩下的三个都卡在"等拍板"上：
   - **P1-4 CMR A 类组条目漏报** —— 影响最大（真漏报、漏 V 级），但修法涉及产品语义
   - **P1-2 UVCB 错误键 9 行** —— 现在仍会用错 SMILES 跑 Cramer，改数据需签字
   - **P1-3 同一 InChIKey 共用** —— 偏严偏松两个方向都有，改核心匹配逻辑

---

## 十二、开工前必须复述给导师/交接人的三件事

1. `fcmsafety-v2` 分支，**不碰 main**；**不用 `rm` / `git rm` / `git stash`**（清文件 `mv` 到 `.workbuddy/`）
2. 改码后 **必须重装包**，否则 `library(fcmsafety)` 看不到改动；自检加 `--no-install`、测试加 `NOT_CRAN=true`、locale 必须 `zh_CN.UTF-8`
3. **审计结论必须调真实函数验证**，不做旁路复刻；**抽样不能外推**（先 `order by random()`）

---

*本文档基于 2026-09-11 的真库状态、`docs/工作总结-20260911.md`、`.workbuddy/memory/` 下的每日日志与长期笔记整理。*
