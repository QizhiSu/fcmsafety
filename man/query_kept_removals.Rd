% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/update_pipeline.R
\encoding{UTF-8}
\name{query_kept_removals}
\alias{query_kept_removals}
\title{Query the Removal Keys That Were Deliberately Kept}
\usage{
query_kept_removals(db_name, db_path = NULL)
}
\arguments{
\item{db_name}{业务表名（\code{"cmr"} / \code{"cmr_suspect"} / \code{"iarc"} / \code{"eu_sml"}）}

\item{db_path}{可选数据库路径}
}
\value{
键的字符向量；账本不存在或无匹配时返回空向量
}
\description{
源里已经找不到、但人工裁定要留在库里的行，键登记在 \code{data_quality_flags}
（\code{flag = "upstream_removed_kept"}）。只有 \code{status = "accepted"} 进白名单 ——
\code{"open"} 表示还没判断过，不能当已知情放行。
}
\details{
\strong{为什么需要这条通道}：这类行删不得。\code{607-230-00-6} 的 CAS 是 \code{-}（"其盐类"
组条目），iarc 的 \code{Progestins} / \verb{Cobalt sulfate} 改名后成了无 CAS 的组条目 ——
三者都拿不到 InChIKey，进不了库。删掉旧行，物质就从库里彻底消失。可留着它们
又会让每一轮更新都报 removed，而 removed > 0 会拦下人工确认闸门（见
\code{\link[=run_incremental_update]{run_incremental_update()}}），等于这四个库永远跑不动。

白名单存在库里而不是写死在代码里：后人查一次 \code{data_quality_flags} 就知道
哪几行是被有意留下的、为什么留，不必重做一遍今天的调查。
}
\keyword{internal}
