% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assign_toxicity.R
\encoding{UTF-8}
\name{query_cmr_data}
\alias{query_cmr_data}
\title{查询 CMR 危险说明代码}
\usage{
query_cmr_data(con, inchikey_list)
}
\arguments{
\item{con}{数据库连接}

\item{inchikey_list}{逗号分隔（各项已带单引号）的 InChIKey 列表，用于 SQL IN}
}
\value{
data.frame(InChIKey, hazard_statement_codes)，每个 InChIKey 一行；
表不存在或查询失败时返回 0 行（调用方据此退化为全部 NA，不中断流程）
}
\description{
cmr 表按 (InChIKey, index_no) 存条目，同一 InChIKey 可能对应多行（同族条目，
如 lead powder 与 lead massive）。若直接用 match() 只取第一行会漏码，故这里
按键把各行的 H 码文本合并成一格，交给 extract_cmr_h_codes() 再拆再归并。
}
\keyword{internal}
