% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/update_dbs.R
\encoding{UTF-8}
\name{update_svhc_auto}
\alias{update_svhc_auto}
\title{SVHC 自动更新（下载 -> diff -> 确认 -> 入库）}
\usage{
update_svhc_auto(
  source = c("auto", "local", "echa"),
  interactive = TRUE,
  auto_apply = FALSE,
  max_auto_changes = 20,
  enrich = TRUE,
  db_path = NULL,
  backup = TRUE,
  update_xlsx = FALSE,
  new_file = NULL
)
}
\arguments{
\item{source}{数据源："auto"（echa -> local）、"local"、"echa"}

\item{interactive}{是否交互确认}

\item{auto_apply}{非交互模式下是否自动入库（受 max_auto_changes 限制）}

\item{max_auto_changes}{自动入库的最大变更条数}

\item{enrich}{是否补 PubChem 化学元数据（只查新增物质，通常 1-5 个请求）}

\item{db_path}{自定义数据库路径（测试用）}

\item{backup}{入库前是否备份数据库}

\item{update_xlsx}{是否将合并结果回写 inst/svhc_meta.xlsx（默认关闭）}

\item{new_file}{显式指定本地清单文件（xlsx/csv）直接读取并更新，
优先于 source 分发；用于 check_manual_lists() 精确消费手动放入的文件}
}
\value{
list(success, changes, db_write)
}
\description{
在 ECHA 官网被 WAF 拦截的现状下，提供"尽量自动"的 SVHC 候选清单更新：
自动抓取数据源、补齐 PubChem 化学信息、与现有数据库做 diff，
确认后以事务方式写入 SQLite（默认自动备份）。
}
\details{
确认规则：
\itemize{
\item interactive = TRUE 时逐项提示 y/n
\item interactive = FALSE 且 auto_apply = TRUE 时，仅当无删除且变更总数
不超过 max_auto_changes 才自动入库（适合无人值守）
\item 出现 removed（清单移除）时，即使 auto_apply 也强制停下人工确认
（SVHC 清单历史上只增不减，removed 通常意味着数据源不完整）
}
}
