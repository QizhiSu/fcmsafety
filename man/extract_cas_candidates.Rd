% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/incremental_update.R
\encoding{UTF-8}
\name{extract_cas_candidates}
\alias{extract_cas_candidates}
\title{从一个单元格里抽出所有可用的 CAS 号}
\usage{
extract_cas_candidates(x)
}
\arguments{
\item{x}{字符向量，元素可为单个 CAS，也可为多值单元格}
}
\value{
list，长度同 \code{x}；每个元素是字符向量（可能为空）
}
\description{
官方导出把多个 CAS 塞进同一格，分隔方式各家不同：CLP 是换行（带 \verb{[n]}
序号标记）、IARC 是 \code{", "}、EU SML 是换行加一长串缩进空格、手工维护的
清单偶尔用空格。\strong{不按分隔符切分}，直接正则捞出所有 CAS 形状的子串再逐个
归一化——切分法会漏掉空格分隔的写法，也会把 \code{"n/a"} 当成一个候选。
}
\details{
返回值是列表，每个元素对应该行清洗后的候选序列（按源顺序、已去重）。
调用方按顺序逐个查，命中即停：CLP 的多 index 条目常常是第一个 CAS 在
PubChem 里没有条目、后面某一组分才有。
}
\keyword{internal}
