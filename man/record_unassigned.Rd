% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/update_pipeline.R
\encoding{UTF-8}
\name{record_unassigned}
\alias{record_unassigned}
\title{Record Entries That Could Not Be Stored}
\usage{
record_unassigned(db_name, dropped, db_path = NULL)
}
\arguments{
\item{db_name}{目标业务表名}

\item{dropped}{\code{\link[=split_unassignable]{split_unassignable()}} 产出的数据框}

\item{db_path}{可选数据库路径}
}
\value{
实际处理的条目数（整数，隐式返回）
}
\description{
把 \code{\link[=split_unassignable]{split_unassignable()}} 摘下来的行写进 \code{unassigned_entries}。同一
(库, 键) 重复遇到只累加 \code{seen_count} 并刷新 \code{last_seen_at}，不会长出重复行。
}
\details{
人工判断优先于自动流程：\code{status = 'accepted'} 的条目不因再次遇到而改回
\code{'open'}，\code{notes} 也一律不覆盖。唯一例外是 \code{'resolved'} 又变回无键 ——
那说明它现在确实收不了（上游撤了 CAS 之类），退回 \code{'open'} 让账本不说谎。

表不存在时先按 schema 补建（见 \code{\link[=ensure_schema_table]{ensure_schema_table()}}），这样早于这张表的
数据库不用整体重建。
}
\keyword{internal}
