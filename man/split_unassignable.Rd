% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/update_dbs.R
\encoding{UTF-8}
\name{split_unassignable}
\alias{split_unassignable}
\title{Split Off Added Rows That Cannot Be Stored}
\usage{
split_unassignable(
  changes,
  db_name,
  key_col,
  fallback_col = NULL,
  cas_col = NULL,
  name_col = NULL,
  looked_up = TRUE
)
}
\arguments{
\item{changes}{\code{\link[=diff_incremental]{diff_incremental()}} 的返回值；只有 \code{added} 会被改写}

\item{db_name}{目标业务表名（写进账本的 \code{database_name}）}

\item{key_col}{主键列名}

\item{fallback_col}{主键为空时的兜底列名}

\item{cas_col}{CAS 列名，用来区分"连 CAS 都没有"和"有 CAS 却查不到结构"}

\item{name_col}{名称列名，写进账本给人看}

\item{looked_up}{本轮是否真的尝试过补结构（即 \code{enrich = TRUE}）。为 \code{FALSE}
时"有 CAS 但没键"只能记成 \code{not_looked_up} —— 那是本轮没去查，不是查不到。
两者混在一起账本就会撒谎：调用方显式传 \code{enrich = FALSE} 时新增行一条都拿
不到键，全记成 \code{no_structure_found} 会让人以为上游没结构。}
}
\value{
\code{list(changes = , dropped = , n_dropped = )}；\code{changes$added} 已剔除
无键行，\code{total_added} 同步改写
}
\description{
上游有一部分条目本体没有结构：IARC 评的感染状态与职业暴露场景
（\code{"Helicobacter pylori (infection with)"}）、A 类组条目
（\code{"salts of hydrazine"}）、UVCB 工业品（\code{"alcohols, aliphatic, ..., (C4-C22)"}）、
反应产物混合物（\code{"reaction mass of: ..."}）。实测四个库合计 414 行属于这一类。
}
\details{
它们拿不到 InChIKey，而业务表的 InChIKey 是 \verb{NOT NULL} 外加指向 \code{chemicals}
的外键 —— 插不进去。写库又是单事务，一行失败连合法行一起回滚（iarc 历史上
因此 846 行全废）。所以这些行在写库前摘出来，交给
\code{\link[=record_unassigned]{record_unassigned()}} 登记成账本。

这是"摘出来"，不是"放宽约束"：这些条目不是暂时查不到结构，而是结构这个
概念对它不成立。塞进按 InChIKey 索引的表，只会让下游以为拿到了结构。
}
\keyword{internal}
