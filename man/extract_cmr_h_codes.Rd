%\encoding{UTF-8}
% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/direct_sql_toxicity.R
\name{extract_cmr_h_codes}
\alias{extract_cmr_h_codes}
\title{从 CLP 危险说明代码文本中提取 CMR 定级相关的 H 码}
\usage{
extract_cmr_h_codes(x, codes = .cmr_h_codes)
}
\arguments{
\item{x}{字符向量，每个元素是 CLP 的一格 H 码文本（可含多个码）}

\item{codes}{目标基础码，默认 6 个 CMR 码，且 V 类在 IV 类之前（决定输出顺序）}
}
\value{
与 x 等长的字符向量：命中的基础码按 codes 顺序用 "; " 连接；
无命中（含 NA、空串、只有非 CMR 码）返回 NA_character_
}
\description{
CLP 的 H 码列是换行分隔的多值文本（本库实测分隔符是两个回车符加一个换行符），且同一个码
带不同后缀（见 .cmr_h_codes 注释）。因此先拆成单个码、统一大写、按前 4 位
（H + 3 位数字）归并，再与 6 个 CMR 码取交集，其余危险（H302 等）一律忽略。
}
\keyword{internal}
