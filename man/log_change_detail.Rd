% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/update_pipeline.R
\encoding{UTF-8}
\name{log_change_detail}
\alias{log_change_detail}
\title{把 diff 结果写成 change_log 逐条明细}
\usage{
log_change_detail(
  con,
  history_id,
  db_name,
  changes,
  old_df = NULL,
  key_fn = NULL,
  key_col = NULL,
  fallback_col = NULL,
  compare_cols = NULL,
  exclude_cols = NULL
)
}
\arguments{
\item{con}{已连接的 SQLite 连接}

\item{history_id}{本次 update_history 记录的 id}

\item{db_name}{库表名}

\item{changes}{diff 输出（含 added / removed / modified 三个 data.frame）}

\item{old_df}{修改前的旧表全量快照（modified 逐字段对比用，可为 NULL）}

\item{key_fn}{行键函数（svhc 用 svhc_key_of；NULL 时按 key_col/fallback_col）}

\item{key_col}{主键列名（key_fn 为 NULL 时使用）}

\item{fallback_col}{兜底键列名}

\item{compare_cols}{参与逐字段对比的列（NULL 时用两表共有业务列，排除
化学元数据 / 系统列；svhc 传 svhc_content_columns）}

\item{exclude_cols}{额外排除的列}
}
\value{
invisible(NULL)；成功时 message 汇总条数
}
\description{
与 write_changes_to_db() / write_svhc_to_db() 配合：在 update_history 记完
总数之后调用。added / removed 每个物质记一行；modified 逐字段对比（需要
old_df 为修改前的旧表快照），每个字段记一行 old_value -> new_value。
明细写入失败不抛出——由调用方 tryCatch 兜底，仅提示，不影响数据入库。
}
\keyword{internal}
