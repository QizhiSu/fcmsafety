%\encoding{UTF-8}
% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/group_membership.R
\name{query_iarc_group_registry}
\alias{query_iarc_group_registry}
\title{构建 IARC 组条目注册表（动态解析 + 可选人工表覆盖）}
\usage{
query_iarc_group_registry(db_path = NULL)
}
\arguments{
\item{db_path}{数据库路径}
}
\value{
data.frame：agent / group_classification / layer / elements /
heuristic_type / smiles_pattern / negative_condition / require_condition /
rationale / representative_formula / representative_smiles

\code{layer} 取 \code{element}（按特征元素粗筛）、\code{valence}（价态，配 SMILES 正则）、
\code{form}（形态，如金属态、粉尘）、\code{scenario}（暴露场景，不可结构判定）或
\code{manual}（拿不出元素判据，元素层直接跳过，见下文）。

\code{negative_condition} 与 \code{require_condition} 是元素级的准入条件：前者表示
"含这些元素就不属于本条"（如无机砷条目排除碳），后者表示"必须含这些元素
才属于本条"（如有机砷条目要求碳）。两者都用分号分隔。
}
\description{
每次运行时从 iarc 表动态筛出组条目，生成基础注册表；
若存在 inst/extdata/iarc_group_manual.xlsx，则用它覆盖 layer / heuristic 等字段。
}
\details{
组条目的筛选有三个来源，取并集：
\enumerate{
\item 名称命中组词正则（\code{compounds} / \code{salts} / \code{metallic} / \code{dust} 等）；
\item \code{cas_no} 为空 —— 类条目通常没有唯一 CAS（扣掉
\code{.iarc_force_single} 里已知的"源表漏填 CAS"的单一物质）；
\item 命中人工核定的 \code{.iarc_extra_group_entries}。
}
}
\keyword{internal}
