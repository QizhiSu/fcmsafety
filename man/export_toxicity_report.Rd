% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/toxicity_report_export.R
\encoding{UTF-8}
\name{export_toxicity_report}
\alias{export_toxicity_report}
\title{Export a toxicity report to Excel (or CSV)}
\usage{
export_toxicity_report(
  result_data,
  path,
  summary = NULL,
  issues = NULL,
  overwrite = TRUE
)
}
\arguments{
\item{result_data}{data.frame returned by \code{\link[=assign_toxicity]{assign_toxicity()}}. Only the
\code{Toxic_level} column is required; everything else is passed through.}

\item{path}{Output path. \code{.xlsx} is written as a styled workbook, \code{.csv} as
a flat file with the same content as the Results sheet.}

\item{summary}{Optional data.frame with columns \code{Section}, \code{Metric}, \code{Value}.
\code{NULL} means derive it from \code{result_data} alone.}

\item{issues}{Optional data.frame with columns \code{Source}, \code{Status}, \code{Rows},
\code{Message}. \code{NULL} means write a single "no issues recorded" row.}

\item{overwrite}{Logical, overwrite an existing file (default: TRUE).}
}
\value{
The output path, invisibly.
}
\description{
Writes the table produced by \code{\link[=assign_toxicity]{assign_toxicity()}} to a workbook with one sheet
per purpose, instead of a single flat CSV:
}
\details{
\describe{
\item{Results}{the full table, one row per input compound, with a frozen
header, an autofilter and fitted column widths. The \code{Toxic_level} cell is
shaded by tier (V dark red through I green, \code{-} grey).}
\item{Summary}{level distribution, database match counts and provenance of
the run (package version, database file and its timestamp, time of run).}
\item{Unassigned}{only the rows whose \code{Toxic_level} is \code{-}, so they can be
worked through separately.}
\item{Issues}{anything that could make the result look cleaner than it is:
a regulatory table that is missing or failed to query, rows listed in
\code{cmr} without usable hazard codes, group-membership rows that need a
human decision.}
}

\code{assign_toxicity(output_file = ...)} calls this automatically when the path
ends in \code{.xlsx}; the function is exported so an existing result data.frame
can be re-exported (for example after editing it by hand) without re-running
the assignment.
}
\examples{
\dontrun{
res <- assign_toxicity(data)
export_toxicity_report(res, "report.xlsx")
}
}
