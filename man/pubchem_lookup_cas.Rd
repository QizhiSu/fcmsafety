% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/update_dbs.R
\encoding{UTF-8}
\name{pubchem_lookup_cas}
\alias{pubchem_lookup_cas}
\title{按 CAS 从 PubChem 查询化学元数据}
\usage{
pubchem_lookup_cas(cas, timeout = 30, retries = 3L, backoff = 1)
}
\arguments{
\item{cas}{CAS 号字符串}

\item{timeout}{单次 HTTP 超时秒数}

\item{retries}{每个请求的重试次数}

\item{backoff}{首次退避秒数}
}
\value{
命名 list，含 CID/Formula/SMILES/InChIKey/IUPACName/ExactMass，
并带 \code{lookup_status} 属性
}
\description{
两步：CAS -> CID，再 CID -> 属性（MolecularFormula、IsomericSMILES、
CanonicalSMILES、InChIKey、IUPACName、ExactMass）。任一步失败或查不到都
返回全 NA 的列表，由调用方决定是否跳过。
}
\details{
返回值带 \code{lookup_status} 属性，取值 \code{"ok"} / \code{"not_found"} / \code{"unavailable"}：
前两者之外，调用方必须能把"PubChem 里没有"与"这次没查成"分开，否则账本
里 \code{no_structure_found} 会撒谎（见 \code{\link[=enrich_new_compounds]{enrich_new_compounds()}}）。
}
\keyword{internal}
