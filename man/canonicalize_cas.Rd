% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/update_dbs.R
\encoding{UTF-8}
\name{canonicalize_cas}
\alias{canonicalize_cas}
\title{规范化 CAS 字符串（去掉各段前导 0、统一分隔符、剥序号标记）}
\usage{
canonicalize_cas(x)
}
\arguments{
\item{x}{字符向量，元素可为单个 CAS，也可为多个 CAS 用 ";"、"\\n"
或 "\\r\\n" 分隔（分隔符由官方导出决定）}
}
\value{
字符向量；无法规范化的元素返回 NA_character_
}
\description{
用于解决不同来源 CAS 格式不一致的问题：有的带前导 0（如
0266309-43-7），有的不带（如 266309-43-7）。规范化后两者相等，
回填和 diff 就不会因为格式差异误判。
}
\details{
多值单元格统一成 \code{";"} 分隔，单元格里各 CAS 的 \verb{[n]} 序号标记
（CLP 官方导出的写法）会被剥掉。注意本函数的职责是\strong{归一化}，不是
"挑一个能用的 CAS"——需要后者的场合用 \code{\link[=extract_cas_candidates]{extract_cas_candidates()}}。
}
\keyword{internal}
