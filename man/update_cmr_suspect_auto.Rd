%\encoding{UTF-8}
% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/update_other_dbs.R
\name{update_cmr_suspect_auto}
\alias{update_cmr_suspect_auto}
\title{CMR_suspect 自动更新}
\usage{
update_cmr_suspect_auto(
  source = c("local", "download"),
  new_file = NULL,
  interactive = TRUE,
  auto_apply = FALSE,
  max_auto_changes = 20,
  enrich = TRUE,
  db_path = NULL,
  backup = TRUE,
  delay = 0.35
)
}
\arguments{
\item{source}{"local"（默认）或 "download"}

\item{new_file}{显式文件路径（可选）}

\item{interactive}{是否交互确认}

\item{auto_apply}{非交互时是否自动入库}

\item{max_auto_changes}{自动入库最大变更条数}

\item{enrich}{是否补 PubChem 化学元数据}

\item{db_path}{自定义数据库路径}

\item{backup}{入库前是否备份}

\item{delay}{PubChem 请求间隔秒数}
}
\value{
list(success, changes, db_write)
}
\description{
与 update_cmr_auto 相同，作用于 cmr_suspect 表（默认读 clp_cmr_meta.xlsx 的
"cmr_suspect" 工作表；source = "download" 时下载原始 CLP 并按 H 代码筛）。
主键 index_no（优先；CLP 官方标识），cas_no 兜底。
}
\details{
主键曾是 substance_name，那是错的：物质名是自由文本，上游改一个连字符、逗号
或省略号就会被判成"删一条 + 加一条"。同一份源上，用 index_no 的 cmr 表报 2 行
removed，用物质名的这张表报 46 行 —— 而那些行一条都没走。库里老行没有 index_no
时要先跑一次 \code{\link[=migrate_cmr_suspect_index_no]{migrate_cmr_suspect_index_no()}}，否则整张表都没有主键。
}
