% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/database.R
\encoding{UTF-8}
\name{ensure_schema_table}
\alias{ensure_schema_table}
\title{Ensure a Single Table Exists, Creating It from the Schema If Missing}
\usage{
ensure_schema_table(table, db_path = NULL, schema_path = NULL)
}
\arguments{
\item{table}{Name of the table to ensure}

\item{db_path}{Optional database path, defaulting to \code{.resolve_db_path()}}

\item{schema_path}{Optional schema file, defaulting to \code{find_schema_file()}}
}
\value{
\code{TRUE} if the table was created, \code{FALSE} if it already existed
(invisibly)
}
\description{
\code{initialize_database()} only reads the schema when the database is created,
so a table added to the schema later never reaches existing databases. This
creates that one table (and the indexes declared for it) on demand, which is
what lets \code{unassigned_entries} appear in a database that predates it without
rebuilding the whole file.
}
\details{
The DDL is taken from the schema file rather than duplicated here, so there
stays exactly one definition of the table. \verb{IF NOT EXISTS} is added on the
way through, which makes repeated calls harmless — the second call is a
no-op and returns \code{FALSE}.
}
\keyword{internal}
