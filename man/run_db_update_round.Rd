%\encoding{UTF-8}
% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_guard.R
\name{run_db_update_round}
\alias{run_db_update_round}
\title{逐库跑一轮更新，返回各库汇总表拼起来的 data.frame}
\usage{
run_db_update_round(dbs, runner, log = NULL, progress = NULL)
}
\arguments{
\item{dbs}{要跑的库名向量，顺序即执行顺序。}

\item{runner}{真正干活的函数，签名 function(db)，返回一行的汇总表；
返回 NULL 或抛错都按"这个库没结果"处理。app 里传的是
\code{update_database_auto()} 的薄包装。}

\item{log}{可选，function(msg)；app 里接到页面上的实时日志通道。}

\item{progress}{可选，function(i, n, db)；app 里接到 incProgress。}
}
\value{
各库结果拼成的 data.frame；一个库都没出结果时返回 NULL。
}
\description{
从 app 里抽出来是为了\strong{能测}：Shiny 的 server 是个闭包，写在里面的循环没法
单独调用，而"某个库失败不能中断整轮""各库结果要能拼成一张表"恰恰是最容易
出错、出错后又最难在页面上发现的地方（只表现为表里少了一行）。
}
\seealso{
\code{\link{summarise_update_run}}
}
\keyword{internal}
