% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/update_dbs.R
\encoding{UTF-8}
\name{diff_svhc_data}
\alias{diff_svhc_data}
\title{与库表比对，生成 diff}
\usage{
diff_svhc_data(new_df, db_path = NULL)
}
\arguments{
\item{new_df}{标准化后的新数据（蛇形列 + 化学列）}

\item{db_path}{数据库路径（NULL 用默认）}
}
\value{
list(added, removed, modified, total_added, total_removed, total_modified)
}
\description{
主键：InChIKey，缺失时退化为 "CAS:\if{html}{\out{<cas>}}"、再退化为 "NAME:\if{html}{\out{<name>}}"。
内容比较只针对 svhc_content_columns（清单核心字段），避免 ECHA 文档
链接列与 PubChem 化学列差异造成误报。
}
\details{
存储模型：svhc 业务表只保留带有效 InChIKey 的行（蛇形 schema），
因此入口先过滤掉无 InChIKey 的新行（UVCB/聚合物等查不到 InChIKey 的
物质不进业务表，默认过滤语义，与 cmr 等库一致）。
}
\keyword{internal}
