% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/update_dbs.R
\encoding{UTF-8}
\name{enrich_new_compounds}
\alias{enrich_new_compounds}
\title{只对新增物质补 PubChem 化学元数据}
\usage{
enrich_new_compounds(
  df,
  cas_col,
  name_col,
  delay = 0.35,
  verbose = TRUE,
  max_cas_try = 5
)
}
\arguments{
\item{df}{标准化后的数据框（含化学列，可为全 NA）}

\item{cas_col}{CAS 列名}

\item{name_col}{名称列名（仅用于进度打印）}

\item{delay}{每次请求间隔秒数}

\item{verbose}{是否打印进度}

\item{max_cas_try}{单行最多试几个候选 CAS}
}
\value{
补全化学列后的 data.frame
}
\description{
增量核心：仅处理"InChIKey 为空 且 CAS 有效"的行（通常是真正新增的物质），
逐行查 PubChem 并写入化学列。已带 InChIKey 的行（如已从库回填）完全跳过，
不发请求。带限速，避免触发 PubChem 封禁。
}
\details{
一个单元格里可能有多个 CAS（CLP 多 index 条目、IARC 组条目、EU SML 聚合物
族），用 \code{\link[=extract_cas_candidates]{extract_cas_candidates()}} 取出候选后\strong{逐个查、命中即停}。只查
第一个会让"主 CAS 在 PubChem 无单体条目、但某组分有"的行整批落空。
取不到任何 CAS 形状的子串时整行跳过，不发请求。

\code{max_cas_try} 给单行的候选数封顶：CLP 有一行写了 31 个 CAS，无上限时
全失败就要连发 31 次请求。序号靠后的成员与整行物质的关联也越来越弱，
用它查到的结构去代表整行本来就牵强。
}
\keyword{internal}
