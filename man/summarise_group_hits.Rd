%\encoding{UTF-8}
% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/direct_sql_toxicity.R
\name{summarise_group_hits}
\alias{summarise_group_hits}
\title{把组条目命中汇总到输入行}
\usage{
summarise_group_hits(hits)
}
\arguments{
\item{hits}{\code{\link[=assign_group_membership_table]{assign_group_membership_table()}} 的结果}
}
\value{
data.frame(input_index, Group_hits, Group_IARC, Group_review)，
每个出现过的 input_index 一行
}
\description{
\code{\link[=assign_group_membership_table]{assign_group_membership_table()}} 返回的是长表：一行输入可能命中多个条目，
一行输入也可能一个都不命中。这里按 \code{input_index} 收成一行，供
\code{\link[=assign_toxicity]{assign_toxicity()}} 左对齐到结果表。
}
\details{
只有 IARC 的组条目带分组，所以只有它会返回可参与定级的 \code{Group_IARC}；
CMR / SVHC 的 UVCB 命中（如"氯化石蜡"）本身不带危险码，只记进 \code{Group_hits}。
置信度为 \code{manual_review} 的命中一律进 \code{Group_review}，不参与定级。
}
\keyword{internal}
