%\encoding{UTF-8}
% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sqlite_database_manager.R
\name{migrate_cmr_suspect_index_no}
\alias{migrate_cmr_suspect_index_no}
\title{Give \code{cmr_suspect} its CLP Index Number and Backfill It}
\usage{
migrate_cmr_suspect_index_no(
  db_path = NULL,
  index_map = NULL,
  meta_file = NULL,
  meta_sheet = "cmr_suspect"
)
}
\arguments{
\item{db_path}{Optional database path, defaulting to \code{.resolve_db_path()}}

\item{index_map}{Optional data frame with \code{substance_name} and \code{index_no}
columns. When \code{NULL}, read from \code{meta_file}.}

\item{meta_file}{Previous migration source; defaults to
\code{inst/clp_cmr_meta.xlsx} next to the schema file.}

\item{meta_sheet}{Worksheet within \code{meta_file}}
}
\value{
\code{list(added = , filled = , unmatched = , total = )}, where \code{added}
says whether the column had to be created and \code{filled} counts the rows that
received a value on this run
}
\description{
\code{cmr_suspect} was migrated without the source's \verb{Index No} column, so the
incremental diff had to fall back to \code{substance_name} as its primary key.
A substance name is free text: when the upstream CLP export re-spells
\verb{O,O-di-methyl} as \verb{O,O-dimethyl}, or writes \code{0.5} as \verb{0,5}, or swaps
\verb{…\%} for \verb{...\%}, the same row is reported as one removal plus one addition.
Measured on the live data: 45 rows were reported removed, of which 26 were
upstream punctuation edits, 15 differed only in whitespace, and 4 came from
upstream merging two entries. The sibling table \code{cmr}, built from the very
same source but keyed on \code{index_no}, reported 2.
}
\details{
\verb{Index No} is the official CLP identifier and is stable across editions. It
is 495/495 non-empty and 495/495 unique in the \code{cmr_suspect} subset of the
current source.

Adding the column is not enough — existing rows have no value for it, and a
NULL key is worse than a mutable one. The values are recovered from the
previous migration source, \code{inst/clp_cmr_meta.xlsx}, whose \code{cmr_suspect}
sheet carries the same \verb{Index No} column and matches all 359 live rows by
name. Rows whose \code{index_no} is already set are left alone, so the function is
idempotent and safe to re-run.
}
\keyword{internal}
