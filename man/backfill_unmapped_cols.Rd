%\encoding{UTF-8}
% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/incremental_update.R
\name{backfill_unmapped_cols}
\alias{backfill_unmapped_cols}
\title{对"新源未提供的列"按 key 回填库中旧值}
\usage{
backfill_unmapped_cols(new_df, cur_df, key_col, fallback_col = NULL)
}
\arguments{
\item{new_df}{map_to_db_columns 后的新数据框（带 mapped_from 属性）}

\item{cur_df}{库表当前全部数据}

\item{key_col}{主键列名}

\item{fallback_col}{兜底键列名（可为 NULL）}
}
\value{
回填后的 data.frame
}
\description{
场景：某业务列在库里有历史值（如旧版 ECHA 导出的备用 H 代码列），而新
版本源文件已不再提供该列（map_to_db_columns 后整列 NA）。若直接入库会把
该列清空并让 diff 把整表报成 modified。此函数在新清单 diff 之前，把这类
列的空白值按 key 从库表当前数据回填，保留历史快照；源已提供的列不受影响。
}
\details{
key 的算法与 diff_incremental() 一致（共用 key_of_df()）：主键为空时用兜底
列，两边必须同键，否则会出现"diff 认得出来、回填找不到"的行。
}
\keyword{internal}
