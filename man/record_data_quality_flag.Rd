% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/update_pipeline.R
\encoding{UTF-8}
\name{record_data_quality_flag}
\alias{record_data_quality_flag}
\title{Record a Data Quality Flag}
\usage{
record_data_quality_flag(
  db_name,
  entries,
  flag,
  severity = "high",
  source = NULL,
  db_path = NULL
)
}
\arguments{
\item{db_name}{业务表名（\code{"cmr"} 等）}

\item{entries}{数据框，须有 \code{entity_key} 列；可选 \code{entity_name}、\code{detail}}

\item{flag}{问题类型标识，如 \code{"unreliable_structure_key"}}

\item{severity}{\code{"high"} / \code{"medium"} / \code{"low"}}

\item{source}{问题出自哪一步，写进表里备查}

\item{db_path}{可选数据库路径}
}
\value{
实际处理的条目数（整数，隐式返回）
}
\description{
把"已入库、但某列已知不可信"的行登记进 \code{data_quality_flags}。与
\code{\link[=record_unassigned]{record_unassigned()}} 同一套约定：同一 (库, 键, 问题类型) 重复遇到只累加
\code{seen_count} 并刷新 \code{last_seen_at}；\code{notes} 与人工设的 \code{status} 不被自动流程
覆盖（\code{'accepted'} 表示人工已判定"就这样，不改"）。
}
\details{
\strong{为什么不写进业务表的 \code{notes} 列}：入库是 DELETE + INSERT，源 df 里没有的
列一律填 \code{NA}（见 \code{\link[=write_changes_to_db]{write_changes_to_db()}}）。只要该行日后被判一次 modified，
写在 \code{notes} 里的标记就被抹掉，且悄无声息 —— 标记本身反而成了不可信的东西。
旁路表不会被写库碰到。

表不存在时先按 schema 补建（见 \code{\link[=ensure_schema_table]{ensure_schema_table()}}），这样早于这张表的
数据库不用整体重建。
}
\keyword{internal}
