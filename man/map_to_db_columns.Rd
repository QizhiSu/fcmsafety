%\encoding{UTF-8}
% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/incremental_update.R
\name{map_to_db_columns}
\alias{map_to_db_columns}
\title{把原始数据框对齐到库表列（动态读库表列名 + 模糊匹配）}
\usage{
map_to_db_columns(raw, db_name, db_path = NULL)
}
\arguments{
\item{raw}{原始数据框（列名可为源文件原始名）}

\item{db_name}{库表名}

\item{db_path}{自定义数据库路径}
}
\value{
列名与库表一致的 data.frame
}
\description{
读库表的实际列名，用 map_source_colname()（精确 -> 归一 -> 候选表）从 raw
取对应列，构造列名与库表完全一致的 data.frame。这样无需硬编码带换行的列名
（如 eu_sml 的 "SML\\n （mg/kg）"），也能正确对齐新旧两代库（蛇形列名如
index_no 会自动命中源文件里的 "Index No"）。raw 中缺失的列（如化学列）置 NA。
系统列（id / created_at / updated_at）由 SQLite 自动维护，不参与源列匹配——
否则 db 列名 "id" 会在模糊匹配的"包含"级误命中任意含 "id" 子串的源列名。
}
\keyword{internal}
