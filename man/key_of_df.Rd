% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/incremental_update.R
\encoding{UTF-8}
\name{key_of_df}
\alias{key_of_df}
\title{实体键：主键为空/占位时退化为"兜底列名: 值"}
\usage{
key_of_df(df, key_col, fallback_col = NULL)
}
\arguments{
\item{df}{数据框}

\item{key_col}{主键列名}

\item{fallback_col}{兜底键列名（可为 NULL 或源/库表里不存在）}
}
\value{
字符向量，无可用键的行返回 NA
}
\description{
diff、回填、变动明细三处必须用同一套键定义。曾经两边不一致：iarc 有 5 行
无 CAS 的组条目（Arecoline / Hexachlorocyclohexanes / Hypochlorite salts）
在 diff 里靠 agent 兜底认了出来，回填却拿空的 cas_no 去找、补不到
InChIKey，写库时撞上 InChIKey 非空约束，导致同批 846 行一起回滚。
}
\details{
键值两侧都先过 canon_key()：源与库对同一实体的书写方式可能只差换行或空格。
}
\keyword{internal}
