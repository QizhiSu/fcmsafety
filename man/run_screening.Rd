% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_screening.R
\encoding{UTF-8}
\name{run_screening}
\alias{run_screening}
\title{一句话跑完筛查：补全结构标识 → 法规匹配与毒性定级 → 导出报告}
\usage{
run_screening(
  data,
  name_col = NULL,
  smiles_col = NULL,
  cas_col = NULL,
  inchikey_col = NULL,
  online = FALSE,
  delay = 0.35,
  output_file = NULL,
  toxtree_result = "toxtree_results.csv",
  group_membership = FALSE,
  db_path = NULL,
  verbose = TRUE
)
}
\arguments{
\item{data}{\code{data.frame}，待筛查物质。至少要有名称列与 SMILES 列。}

\item{name_col, smiles_col, cas_col, inchikey_col}{字符标量。显式指定列名；
\code{NULL}（默认）时按常见列名自动识别。\code{cas_col} / \code{inchikey_col} 可为空。}

\item{online}{逻辑值。本地推导不出 \code{InChIKey} 时是否联网 PubChem 兜底。
\strong{默认 \code{FALSE}}（全离线）。打开后每行会按 \code{delay} 间隔请求，较慢。}

\item{delay}{数值。联网兜底时每行之间的间隔秒数，默认 0.35。}

\item{output_file}{字符标量或 \code{NULL}。报告输出路径，\strong{按扩展名分派}：
\code{.xlsx} 走 \code{\link[=export_toxicity_report]{export_toxicity_report()}}（Results / Summary / Unassigned /
Issues 四张表），\code{.csv} 走 \code{write.csv}。\code{NULL} 时只返回结果不落盘。}

\item{toxtree_result}{字符标量。Toxtree（Cramer 规则）结果 CSV 的路径。
文件已存在则直接读取；不存在但数据含 \code{SMILES} 列时会现场调用
\code{\link[=run_toxtree]{run_toxtree()}} 生成（首次需要联网下载 jar，约 81MB）。默认
\code{"toxtree_results.csv"}（当前工作目录）。}

\item{group_membership}{逻辑值。是否额外做 IARC 等\strong{组条目}归属判定
（见 ADR 0009 / 0010）。默认 \code{FALSE}，因为组条目匹配是启发式的、
且会显著加长运行时间。}

\item{db_path}{字符标量或 \code{NULL}。SQLite 数据库路径；\code{NULL} 用包内默认库。}

\item{verbose}{逻辑值。是否打印补全过程的进度信息，默认 \code{TRUE}。}
}
\value{
\code{data.frame}，即 \code{\link[=assign_toxicity]{assign_toxicity()}} 的结果表（在输入列基础上追加
\code{SVHC} / \code{CMR} / \code{Cramer_rules} / \code{Toxic_level} 等列）。
\code{prepare_input()} 的补全报告挂在 \code{attr(result, "prepare_report")} 上，
可用 \code{\link[=print_prepare_report]{print_prepare_report()}} 查看。
}
\description{
把 \code{\link[=prepare_input]{prepare_input()}} 与 \code{\link[=assign_toxicity]{assign_toxicity()}} 串成一个入口，省掉手工接力的两步。
内部只做调度与参数透传，不复制两者的任何逻辑。
}
\details{
输入通常只需\strong{名称 + SMILES} 两列：\code{SMILES} 用来本地推导 \code{InChIKey}
（离线，见 ADR 0004），\code{NAME} 用来在库内做名称匹配补齐元数据。
若数据已自带 \code{InChIKey} 列，则直接采用，跳过推导。

\strong{本函数不联网、不改数据库。} 结构推导默认全离线；法规匹配只读本地
SQLite。\code{assign_toxicity()} 上那两个会触碰"人工清单文件"的参数
（\code{check_updates} / \code{auto_update}）刻意没有暴露出来 —— 它们是写库语义，
不属于确定性匹配入口。
}
\examples{
\dontrun{
# 最小用法：只要有名称和 SMILES
substances <- data.frame(
  NAME   = c("Bisphenol A", "Ethanol"),
  SMILES = c("CC(C)(c1ccc(O)cc1)c1ccc(O)cc1", "CCO")
)
result <- run_screening(substances, output_file = "screening.xlsx")

# 看补全情况（哪些行没解析出结构）
print_prepare_report(result)

# 本地解析不了的，联网兜底
result <- run_screening(substances, online = TRUE)
}
}
\seealso{
\code{\link[=prepare_input]{prepare_input()}}、\code{\link[=assign_toxicity]{assign_toxicity()}}、\code{\link[=export_toxicity_report]{export_toxicity_report()}}
}
